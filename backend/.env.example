# Copy to .env and adjust as needed.

PORT=4000

# MongoDB connection string.
# If you leave this UNSET, the server automatically spins up an in-memory
# MongoDB (via mongodb-memory-server) so the app runs with zero setup.
# For a real database use something like:
MONGODB_URI=mongodb://localhost:27017/interviewScheduler

